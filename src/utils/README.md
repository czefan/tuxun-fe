# 通用工具函数 (`src/utils`)

本目录存放纯无状态工具函数。

---

## ⚠️ 编写约束

1. **纯函数**：所有工具必须为无副作用的纯函数（Pure Functions），便于单元测试覆盖。
2. **严禁依赖 UI / Vue 实例**：不得包含 `ref`、`reactive` 或 Vue 生命周期钩子。
3. **业务工具下沉**：有具体业务语义的逻辑应放入对应 `src/features/*` 业务域中。
